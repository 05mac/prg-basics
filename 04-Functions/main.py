import calendar # Importujemy nasz własny moduł (bez .py)

# Część 1: Wypisanie nazwy miesiąca nr 7 (zgodnie z treścią zadania)
n = 7
result = calendar.month(n)
print(f"The name of month {n} is {result}")

print("-" * 30)